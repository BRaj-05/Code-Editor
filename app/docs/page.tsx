"use client";
import { useState } from "react";
import Link from "next/link";
import { Search } from "lucide-react";
const sections = [["getting-started","Getting Started","Create a project from Dashboard, choose a template, then select a file in Explorer. The editor is usable while the browser runtime prepares dependencies."],["projects","Projects","Projects preserve their template files. Use Save for the active file or Save All for every dirty tab."],["templates","Templates","React, Next.js, Vue, Angular, Express, and Hono are included. Runtime detection checks dev, start, serve, then preview."],["editor","Editor","Monaco provides highlighting, diagnostics, tabs, breadcrumbs, word wrap, minimap, and configurable font and tab sizes."],["terminal","Terminal","The terminal shares the active WebContainer. Runtime logs remain visible when installation or startup fails."],["preview","Preview","Preview opens only after a valid server-ready URL. Switch between desktop, tablet, and mobile widths."],["shortcuts","Keyboard Shortcuts","Ctrl/Cmd+S saves, Ctrl/Cmd+P opens files, Ctrl/Cmd+Shift+P opens commands, and Ctrl/Cmd+J toggles terminal."],["runtime","Runtime","One WebContainer is reused per workspace. Dependencies reinstall only when package.json or a lockfile changes."],["troubleshooting","Troubleshooting","Read Terminal output and use Retry. Missing scripts now show an actionable error. Servers must listen on 0.0.0.0."]] as const;
export default function DocsPage() { const [query,setQuery]=useState(""); const visible=sections.filter(([,title,body])=>`${title} ${body}`.toLowerCase().includes(query.toLowerCase())); return <div className="min-h-screen bg-[#09090b] text-zinc-100"><header className="sticky top-0 z-20 border-b border-white/10 bg-[#09090b]/90 backdrop-blur"><div className="mx-auto flex h-14 max-w-7xl items-center px-5"><Link href="/" className="font-semibold">VibeCode</Link><span className="mx-3 text-zinc-700">/</span><span className="text-sm text-zinc-400">Docs</span></div></header><div className="mx-auto grid max-w-7xl md:grid-cols-[230px_1fr]"><aside className="hidden min-h-[calc(100vh-3.5rem)] border-r border-white/10 p-5 md:block"><p className="mb-4 text-[10px] font-bold text-zinc-500">DOCUMENTATION</p><nav className="grid gap-1">{sections.map(([id,title])=><a key={id} href={`#${id}`} className="rounded-sm px-2 py-1.5 text-xs text-zinc-400 hover:bg-white/5 hover:text-white">{title}</a>)}</nav></aside><main className="min-w-0 px-6 py-10 md:px-12"><h1 className="text-4xl font-bold">Build with VibeCode</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-zinc-400">Create projects, learn the editor, and understand the browser runtime.</p><label className="mt-8 flex max-w-xl items-center gap-2 border border-white/10 bg-[#111318] px-3"><Search size={15} className="text-zinc-500"/><span className="sr-only">Search documentation</span><input value={query} onChange={event=>setQuery(event.target.value)} placeholder="Search documentation..." className="h-11 w-full bg-transparent text-sm outline-none"/></label><div className="mt-12 max-w-3xl divide-y divide-white/10">{visible.map(([id,title,body])=><section id={id} key={id} className="scroll-mt-20 py-8"><h2 className="text-xl font-semibold">{title}</h2><p className="mt-3 text-sm leading-7 text-zinc-400">{body}</p></section>)}{!visible.length&&<p className="py-12 text-sm text-zinc-500">No documentation matches that search.</p>}</div></main></div></div>; }
